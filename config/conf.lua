return {
  -- The tcp port to listen too
  port = 8000,

  -- The absolute path to the unix domain socket to use
  -- socket = '/var/run/why.sock',

  -- The absolute path to where your files exist
  document_root = '/var/www/html'
}

